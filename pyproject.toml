[project]
name = "akibaflow"
version = "0.1.0"
description = "AkibaFlow API"
requires-python = ">=3.12"

dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "alembic",
  "psycopg2-binary",
  "pydantic-settings",
]
